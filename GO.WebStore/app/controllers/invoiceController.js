'use strict';
app.controller('invoiceController', ['$scope', '$routeParams', 'authService', function ($scope, $routeParams, authService) {

    //console.log('qweqweqweqweqweq');

    //if ($routeParams.code !== null) {

    //    var sale = $routeParams.code;

    //    console.log(sale);

    //    getInvoice(sale);
    //}

    //function getInvoice(sale) {
    //    gostoFactory.obterVenda(sale).success(function (results) {
    //        $scope.invoice = results;
    //    });
    //}
}]);